server:
  port: ${APPLICATION_PORT}
  servlet:
    context-path: /api/v1

spring:
  application:
    name: backend

  profiles:
    active: dev

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    baseline-version: 0

  jpa:
    open-in-view: false